# 开发环境专用 Dockerfile - 支持热更新
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY package*.json ./

# 设置npm源并安装依赖
RUN npm config set registry https://registry.npmmirror.com/ && \
    npm install

# 复制源代码（开发时通过volume挂载，这里只是初始化）
COPY . .

# 暴露端口
EXPOSE 3000

# 开发模式启动
CMD ["npm", "run", "serve", "--", "--host", "0.0.0.0", "--port", "3000"]